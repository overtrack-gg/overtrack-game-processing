[tool.poetry]
name = "overtrack"
version = "0.1.0"
description = ""
authors = ["Simon Pinfold <simon@uint8.me>"]

[tool.poetry.dependencies]
python = "^3.6"

setuptools = "^40"

# base requirements
requests = "^2"
dataclasses = "*"
python-dateutil = "^2.7"
shortuuid = "*"
typedload = "*"

# embedded web servers
sanic = "^0.8"

# ingest instance management
python-digitalocean = { git = "https://github.com/koalalorenzo/python-digitalocean.git" }

# image/data processing
numpy = "^1"
opencv-python = "^3.4"
tensorflow = "^1.10"
tesserocr = "^2.2"
editdistance = "*"

# video/stream/vod extraction
av = { git = "https://github.com/mikeboers/PyAV.git"}
#av = { file = "av-6.0.0-cp36-cp36m-win_amd64.whl" }
streamlink = { git = "https://github.com/streamlink/streamlink.git" }

# overtrack.gg game upload support
pynamodb = "^3.3"

sentry-sdk = "^0.5"
google-cloud-logging = "^1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
